package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Bento Grid Design Palette (Light Theme)
val SlateBg = Color(0xFFFDF8F6)        // Bento App Background (Warm Cream/Off-white)
val CardSlate = Color(0xFFFFFFFF)      // Bento Grid Card Surface (Pure White)
val CardSlateLight = Color(0xFFE6E1E5) // Bento Card Border & Inner Containers (Warm Lavender-Gray)

val AardvarkCyan = Color(0xFFFF6321)   // Bento Coral Orange (Primary Accent Action / Branding)
val AardvarkCyanLight = Color(0xFFFF9E7A) // Bento Soft Light Coral
val AardvarkSteel = Color(0xFF21005D)  // Bento Deep Indigo (Secondary Brand Block / Dark Accent)

val TextWhite = Color(0xFF1C1B1F)      // Bento Dark Charcoal (Primary typography / High contrast)
val TextGray = Color(0xFF49454F)       // Bento Dark Slate Gray (Secondary body text / Descriptions)
val TextMuted = Color(0xFF6750A4)      // Bento Medium Purple (Muted subtitle / Category label)

// Status colors aligned with Material 3 Bento theme
val StatusGreen = Color(0xFF1976D2)     // M3 Blue for Info/MFA indicator
val StatusGreenBg = Color(0xFFD3E3FD)   // Soft Light Blue container
val StatusRed = Color(0xFFB3261E)       // M3 Error Red
val StatusRedBg = Color(0xFFF9DEDC)     // Soft Error Red container
val StatusOrange = Color(0xFFD0BCFF)    // M3 Lavender Accent
val StatusOrangeBg = Color(0xFFEADDFF)  // Soft Light Lavender container
val StatusBlue = Color(0xFF6750A4)      // M3 Purple Accent
val StatusBlueBg = Color(0xFFF3EDF7)    // Soft Light Purple container
